import Profile from '@/modules/profile';

const routes = [
	{
		path: 'profile',
		name: 'profile',
		component: Profile,
		meta: {
            title: 'Profile'
        }
	}
];

export default routes;
