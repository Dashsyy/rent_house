import Dashboard from '@/modules/dashboard';

const routes = [
	{
		path: 'dashboard',
		name: 'dashboard',
		component: Dashboard,
		meta: {
			menu_key: 'dashboard'
		}
	}
];

export default routes;
